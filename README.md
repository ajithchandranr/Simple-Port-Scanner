# Simple-Port-Scanner
The Simple Port Scanner is a lightweight Python script for scanning open ports on a target host. It provides a basic way to identify open ports through TCP connection attempts. Useful for network analysis or security testing.
